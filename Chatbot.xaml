<UserControl x:Class="MiBankito.Chatbot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="DeepOrange" SecondaryColor="LightBlue" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MiBankito;component/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ChatDataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
                <Setter Property="Background" Value="{StaticResource PrimaryDarkBlueBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Height" Value="34"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundSecondaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- WhatsApp-style Header -->
        <Border Grid.Row="0" Background="{StaticResource SuccessGreenBrush}" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Kind="Whatsapp" Width="40" Height="40" Foreground="White" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Solicitudes WhatsApp" FontSize="20" FontWeight="SemiBold" Foreground="White" />
                    <TextBlock Text="Conversaciones y solicitudes entrantes" FontSize="12" Foreground="#E6FFE8" />
                </StackPanel>

                <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconButton}" ToolTip="Volver" Click="Cancel_Click">
                    <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="White"/>
                </Button>
            </Grid>
        </Border>

        <!-- Body -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">


                <!-- Transactions list -->
                <materialDesign:Card Padding="12" Background="{StaticResource BackgroundWhiteBrush}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <materialDesign:PackIcon Kind="Table" Width="22" Height="22" Foreground="{StaticResource PrimaryDarkBlueBrush}" Margin="0,0,8,0"/>
                            <TextBlock Text="Transacciones recibidas por Chatbot" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>

                        <DataGrid x:Name="ChatTransactionsGrid"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F2FFF4"
                                  ColumnHeaderStyle="{StaticResource ChatDataGridColumnHeaderStyle}">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="" Width="40">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <materialDesign:PackIcon Kind="Whatsapp" Width="18" Height="18" Foreground="{StaticResource SuccessGreenBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Fecha" Width="150" Binding="{Binding Timestamp, StringFormat=g}" />
                                <DataGridTextColumn Header="Servicio" Width="200" Binding="{Binding Service}" />
                                <DataGridTextColumn Header="Referencia" Width="100" Binding="{Binding Reference}" />
                                <DataGridTextColumn Header="Monto" Width="100" Binding="{Binding Amount, StringFormat=C2}" />
                                <DataGridTextColumn Header="ID Cajero" Width="100" Binding="{Binding CashierId}" />

                                <!-- Actions column: Confirm / Reject -->
                                <DataGridTemplateColumn Header="Acciones" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Content="Confirmar" Style="{StaticResource MaterialDesignRaisedButton}" Background="{StaticResource PrimaryDarkBlueBrush}" Foreground="White" Margin="0,0,8,0" Click="ConfirmButton_Click"/>
                                                <Button Content="Rechazar" Style="{StaticResource MaterialDesignOutlinedButton}" Click="RejectButton_Click"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Button x:Name="LoadSampleBtn" Content="CARGAR TRANSACCIONES" Background="#25D366" Foreground="White" BorderBrush="#25D366" Margin="0,12,0,0" HorizontalAlignment="Right" Click="LoadSampleBtn_Click"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>